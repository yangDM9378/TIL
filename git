
git log git의 comit hisory 보기
git diff 두 commit 간 차이 보기

git commit -m "a,b 추가"
git status

작업공간
git add
특정버전 관리 파일 보관
git commit
커밋들이 저장되는 곳

github repository--local repository



clone

git add .  전체 파일 추가
git add test.txt 특정한 파일 추가

git commit -m "test 파일 추가" 이름 추가

git push 깃허브에 올리기


로그인하기
git config --global user.email coco1622555@gmail.com 
git config --global user.name yangDM9378